{
  "name": "armo-test-task",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start-win": "npm install && pip install tornado && start cmd /k call python3 server.py && electron . ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron": "^13.1.7",
    "node-fetch": "^2.6.1"
  }
}
